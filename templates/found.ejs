<%- include('header', { socials: false }) %>

<% if (duck == null) { %>

<p>Unkown duck "<%= duckId %>". Did you spell it right?</p>

<% } else { %>

<div id="found">
  <p>You found a duck!</p>
  <%- include('duck', { duck }); %>
  <% const tw_href = 'https://twitter.com/intent/tweet?text=' + encodeURIComponent(`@ducks_umn I just found ${duck.name} (#${duck.id})`);
     const ig_href = ''; %>
  <p>Please join the duck hunt! <a href="<%= tw_href %>">Tweet at</a> or <a href="<%= ig_href %>">tag us on IG</a>.</p>
  <p>Go <a href="/">home</a> for more ducks</p>
</div>

<style>

#found {
  text-align: center;
}

</style>

<% } %>

<%- include('footer') %>
